NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=development-anon-key-replace-me
APP_ALLOWED_EMAIL=matthewirving99@gmail.com
# Public browser values are limited to the Supabase URL and anon key.
# Server-only values (service-role key, project ref, database password, access tokens,
# Cloudflare API token and R2 credentials) belong in local secret management, never this file.
