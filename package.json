{
  "name": "ai-operations",
  "private": true,
  "packageManager": "pnpm@10.18.2",
  "engines": {
    "node": ">=22.18.0",
    "pnpm": ">=10.18.0"
  },
  "scripts": {
    "dev": "corepack pnpm --filter @ai-operations/web dev",
    "build": "corepack pnpm --filter @ai-operations/web build",
    "format": "prettier --write .github apps/web packages docs infrastructure supabase tests package.json pnpm-workspace.yaml turbo.json tsconfig.base.json playwright.config.ts README.md .prettierrc.json",
    "format:check": "prettier --check .github apps/web packages docs infrastructure supabase tests package.json pnpm-workspace.yaml turbo.json tsconfig.base.json playwright.config.ts README.md .prettierrc.json",
    "lint": "corepack pnpm -r --if-present run lint",
    "typecheck": "corepack pnpm -r --if-present run typecheck",
    "test": "corepack pnpm -r --if-present run test",
    "test:db": "supabase test db",
    "test:e2e": "playwright test",
    "security": "corepack pnpm audit --audit-level=high && gitleaks detect --no-banner",
    "verify": "corepack pnpm format:check && corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test && corepack pnpm build",
    "preflight": "tsx infrastructure/scripts/pass-preflight.ts",
    "finish-pass": "tsx infrastructure/scripts/pass-finish.ts"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "22.15.3",
    "@vitest/coverage-v8": "3.2.6",
    "prettier": "3.6.2",
    "supabase": "^2.111.0",
    "tsx": "4.20.3",
    "turbo": "2.5.6",
    "typescript": "5.8.3",
    "vitest": "3.2.6"
  },
  "pnpm": {
    "overrides": {
      "playwright": "1.61.1",
      "postcss": "8.5.25",
      "sharp": "0.35.2",
      "vitest": "3.2.6"
    }
  }
}
