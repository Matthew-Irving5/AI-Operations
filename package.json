{
  "name": "ai-operations",
  "private": true,
  "packageManager": "pnpm@10.18.2",
  "engines": {
    "node": ">=22.18.0",
    "pnpm": ">=10.18.0"
  },
  "scripts": {
    "dev": "corepack pnpm --filter @ai-operations/web dev",
    "build": "corepack pnpm --filter @ai-operations/web build",
    "format": "prettier --write .github apps/web packages docs infrastructure supabase/migrations supabase/tests tests package.json pnpm-workspace.yaml turbo.json tsconfig.base.json playwright.config.ts README.md .prettierrc.json",
    "format:check": "prettier --check .github apps/web packages docs infrastructure supabase/migrations supabase/tests tests package.json pnpm-workspace.yaml turbo.json tsconfig.base.json playwright.config.ts README.md .prettierrc.json",
    "lint": "corepack pnpm -r --if-present run lint",
    "typecheck": "corepack pnpm -r --if-present run typecheck",
    "test": "corepack pnpm -r --if-present run test",
    "test:db": "supabase test db",
    "test:e2e": "tsx infrastructure/scripts/run-e2e.ts",
    "security": "corepack pnpm audit --audit-level=high && gitleaks detect --no-banner",
    "verify": "corepack pnpm format:check && corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test && corepack pnpm build",
    "cloudflare:check": "corepack pnpm --dir apps/web run deploy -- --dry-run",
    "verify:ci": "tsx infrastructure/scripts/verify-ci.ts",
    "preflight": "tsx infrastructure/scripts/pass-preflight.ts",
    "finish-pass": "tsx infrastructure/scripts/pass-finish.ts"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.61.1",
    "@types/node": "22.15.3",
    "@vitest/coverage-v8": "3.2.6",
    "prettier": "3.6.2",
    "supabase": "^2.111.0",
    "tsx": "4.20.3",
    "turbo": "2.5.6",
    "typescript": "5.8.3",
    "vitest": "3.2.6"
  },
  "pnpm": {
    "overrides": {
      "playwright": "1.61.1",
      "postcss": "8.5.25",
      "sharp": "0.35.2",
      "undici": "7.29.0",
      "vitest": "3.2.6"
    }
  }
}
