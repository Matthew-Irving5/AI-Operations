# Shared configuration

This directory owns shared repository configuration conventions. TypeScript packages extend the root strict configuration, while Prettier and workspace configuration remain at the repository root so every tool has one source of truth.
