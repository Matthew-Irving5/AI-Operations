# Backup and restore

Production backups are encrypted and stored separately from operational data. Restore drills use staging only, synthetic data only, and require a checksum-verified archive manifest. A production restore requires a documented approval and fresh MFA.
