# Cost controls

No AI call is enabled in Pass 1. Future workflows must reserve deterministic budget before model invocation, enforce hard caps in database transactions, and record estimated and actual cost. Browser clients never receive provider credentials.
