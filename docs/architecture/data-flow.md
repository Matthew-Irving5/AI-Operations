# Data flow

Ingestion adapters validate data, hash and archive raw bytes, then write queryable records. Workflows only consume authorised sources and append redacted trace and audit events. Reports and action proposals remain user-owned, RLS-protected records.
