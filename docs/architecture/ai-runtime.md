# AI runtime boundary

The platform uses the OpenAI Responses API through server-side Edge Functions in later passes. Every call will require strict structured output validation, model routing, a deterministic budget reservation, redacted tracing, and source evidence.
